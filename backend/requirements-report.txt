# ===============================================
# PDF Report Generation Dependencies
# ===============================================
# This file contains optional dependencies for PDF report generation.
# Install with: pip install -r requirements-report.txt
#
# System packages required (Ubuntu/Debian):
#   sudo apt-get install libcairo2-dev libpango1.0-dev fonts-nanum fonts-nanum-coding
#
# macOS:
#   brew install cairo pango gdk-pixbuf libffi
#   brew install font-nanum
#
# Windows:
#   Install GTK+ runtime and NanumGothic font manually

weasyprint>=60.0
cssselect2>=0.7.0
tinycss2>=1.2.0
Pillow>=10.0.0  # Already in NGC base but ensure version
