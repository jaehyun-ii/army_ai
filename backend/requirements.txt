# ===============================================
# Army AI Platform - GPU Requirements
# For use with nvcr.io/nvidia/pytorch:25.10-py3
# ===============================================
# NOTE: The following are already included in NGC base image:
#       PyTorch, torchvision, torchaudio, CUDA, cuDNN,
#       NumPy, Pillow, matplotlib, pandas, scikit-learn,
#       tqdm, pyyaml, scipy, opencv-python

# Core Dependencies
# ==================

# FastAPI & Server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.20

# Database
sqlalchemy>=2.0.36
asyncpg>=0.30.0
psycopg2-binary>=2.9.10

# Validation & Settings
pydantic>=2.10.0
pydantic-settings>=2.7.0
email-validator>=2.2.0

# Authentication & Security
python-jose[cryptography]>=3.3.0
PyJWT>=2.10.0
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1 
python-dateutil>=2.9.0

# Environment & Config
python-dotenv>=1.0.1
# pyyaml: Already in NGC base image

# Image Processing
# opencv-python: Already in NGC base image

# ===============================================
# Deep Learning Libraries
# ===============================================
# PyTorch, torchvision, torchaudio, Pillow: Already in NGC base image ✅
# NumPy: Already in NGC base image
# opencv-python: Already in NGC base image
ultralytics==8.3.40
# scikit-learn: Already in NGC base image
# matplotlib: Already in NGC base image
# tqdm: Already in NGC base image
# pandas: Already in NGC base image

# System monitoring
psutil>=6.0.0
nvidia-ml-py>=12.560.30  # pynvml - NVIDIA Management Library for GPU monitoring