# GT (Ground Truth) 처리 로직 수정 완료

**날짜**: 2025-12-24
**목적**: 2D/3D 데이터셋 평가 시 GT 소스를 올바르게 처리

---

## ✅ 최종 요구사항

### 시나리오별 GT 사용

| 시나리오 | 2D | 3D |
|---------|----|----|
| **1. 베이스만 선택** | 베이스 GT로 평가 | 베이스 GT로 평가 |
| **2. 공격만 선택** | **베이스 GT**로 평가 (auto-populate) | **공격 자체 GT**로 평가 |
| **3. 둘다 선택** | **베이스 GT**로 둘 다 평가 | **각각의 GT**로 평가 |

---

## 🔍 핵심 차이점

### 2D Attack Dataset
```
원본 이미지 → [패치/노이즈 추가] → 공격 이미지

특징:
- 객체 위치 불변 (perturbation만 추가)
- GT는 원본과 동일
- filename mapping으로 원본 GT 재사용
```

**예시**:
```
original/person_001.png  GT: [person at (100,100,200,200)]
attacked/person_001.png  GT: [person at (100,100,200,200)] ← 동일!
```

### 3D Attack Dataset (CARLA)
```
CARLA Simulator → [패치 적용 + 새 장면 생성] → 공격 이미지 + GT

특징:
- 새로운 장면 생성 (카메라 위치, 조명, 객체 위치 등 모두 새로 생성)
- GT도 CARLA에서 새로 생성
- output_dataset에 이미지 + annotations 포함
```

**예시**:
```
attack_output/frame_001.png  GT: [person at (150,120,250,220)]  ← CARLA 생성
```

---

## ❌ 이전 구현의 문제

**파일**: `backend/app/services/evaluation_service.py:442-474`

```python
# ❌ 이전 코드 (잘못됨)
if original_images:
    # 원본 GT 사용 (2D/3D 구분 없이)
    attack_metrics = await self._calculate_metrics_with_gt(
        images=original_images,  # ❌ 3D에서도 원본 GT 사용
        use_filename_mapping=True,
    )
else:
    # 공격 자체 GT 사용
    attack_metrics = await self._calculate_metrics_with_gt(
        images=attack_images,
        use_filename_mapping=False,
    )
```

**문제점**:
1. ❌ 2D와 3D를 구분하지 않음
2. ❌ 3D 둘다 선택 시 (`original_images` 존재): 원본 GT를 filename mapping으로 사용
3. ❌ 3D 공격 이미지는 CARLA에서 새로 생성되므로 원본 GT와 매칭 안 됨
4. ❌ Filename pattern도 다를 수 있음 (original_001.png vs frame_001.png)

---

## ✅ 수정된 구현

**파일**: `backend/app/services/evaluation_service.py:439-481`

```python
# ✅ 수정된 코드 (올바름)
# GT selection logic differs by dimension:
# - 2D: Always use original GT (filename mapping) - objects don't move, only perturbed
# - 3D: Always use attack dataset's own GT - CARLA generates new GT for each attack

if dimension == "3d":
    # 3D: Attack dataset has its own GT generated by CARLA
    await eval_logger.info("3D 공격 데이터셋 자체 GT 사용 (CARLA 생성)")
    attack_metrics = await self._calculate_metrics_with_gt(
        db=db,
        inference_results=attack_inference_results,
        images=attack_images,  # ✅ Use attack dataset's own images for GT
        dataset=attack_dataset_obj,  # ✅ Use attack dataset for class names
        use_filename_mapping=False,  # ✅ Direct image_id matching
        target_class=target_class,
        iou_threshold=iou_threshold,
        dimension=dimension,
    )
else:
    # 2D: Use original GT (object positions unchanged, only perturbed)
    if original_images:
        await eval_logger.info("2D 공격 이미지 평가 (원본 GT 사용, filename mapping)")
        attack_metrics = await self._calculate_metrics_with_gt(
            db=db,
            inference_results=attack_inference_results,
            images=original_images,  # ✅ Use original images for GT
            dataset=original_dataset,  # ✅ Use original dataset for class names
            use_filename_mapping=True,  # ✅ Map by filename
            target_class=target_class,
            iou_threshold=iou_threshold,
            dimension=dimension,
        )
        # Check if mapping was successful
        has_metrics = (...)
        if not has_metrics:
            raise ValidationError("공격 이미지와 원본 이미지 매핑 실패")
    else:
        # Should not happen for 2D (base_dataset_id is auto-populated)
        raise ValidationError("2D 공격 데이터셋 평가에는 base_dataset_id가 필요합니다.")
```

---

## 📊 시나리오별 동작 확인

### ✅ 시나리오 1: 베이스만 선택

**2D/3D 공통**:
```python
# _evaluate_base_dataset()
dataset = base_dataset
images = base_dataset_images
inference_results = await run_inference(images)

metrics = await _calculate_metrics_with_gt(
    inference_results=inference_results,
    images=images,  # ✅ 베이스 GT 사용
    dataset=dataset,
    use_filename_mapping=False,  # Direct image_id match
)
```

**결과**: ✅ 베이스 GT로 평가

---

### ✅ 시나리오 2: 공격만 선택

#### 2D
```python
# _evaluate_attack_dataset()
original_dataset_id = attack_dataset.base_dataset_id  # ✅ Auto-populated
original_dataset = await get_dataset(original_dataset_id)
original_images = await get_images(original_dataset_id)

attack_dataset_obj = await get_dataset(attack_output_dataset_id)
attack_images = await get_images(attack_output_dataset_id)

# Step 1: Evaluate original
original_metrics = await _calculate_metrics_with_gt(
    images=original_images,  # ✅ 원본 GT
)

# Step 2: Evaluate attack
if dimension == "2d":  # ✅
    attack_metrics = await _calculate_metrics_with_gt(
        images=original_images,  # ✅ 원본 GT 사용
        use_filename_mapping=True,  # filename으로 매칭
    )
```

**결과**: ✅ 베이스 GT로 평가

#### 3D
```python
# _evaluate_attack_dataset()
original_dataset_id = eval_run.base_dataset_3d_id  # May be None
if not original_dataset_id:
    original_dataset = None
    original_images = None

attack_dataset_obj = await get_dataset(attack_output_dataset_id)
attack_images = await get_images(attack_output_dataset_id)

# Step 1: Evaluate original (skip if None)
if original_dataset_id:
    original_metrics = await _calculate_metrics_with_gt(...)
else:
    original_metrics = None

# Step 2: Evaluate attack
if dimension == "3d":  # ✅
    attack_metrics = await _calculate_metrics_with_gt(
        images=attack_images,  # ✅ 공격 자체 GT 사용
        dataset=attack_dataset_obj,
        use_filename_mapping=False,
    )
```

**결과**: ✅ 공격 자체 GT로 평가

---

### ✅ 시나리오 3: 둘다 선택

#### 2D
```python
# Frontend creates 2 runs:
# Run 1 (pre_attack): base only → 베이스 GT 사용 ✅
# Run 2 (post_attack): base + attack

# Run 2:
original_dataset = base_dataset
original_images = base_images
attack_dataset_obj = attack_output_dataset
attack_images = attack_output_images

# Step 1: Evaluate original
original_metrics = await _calculate_metrics_with_gt(
    images=original_images,  # ✅ 베이스 GT
)

# Step 2: Evaluate attack
if dimension == "2d":  # ✅
    attack_metrics = await _calculate_metrics_with_gt(
        images=original_images,  # ✅ 베이스 GT (filename mapping)
        use_filename_mapping=True,
    )
```

**결과**: ✅ 베이스 GT로 둘 다 평가

#### 3D
```python
# Frontend creates 2 runs:
# Run 1 (pre_attack): base only
# Run 2 (post_attack): base + attack

# Run 2:
original_dataset = base_dataset_3d
original_images = base_images
attack_dataset_obj = attack_output_dataset
attack_images = attack_output_images

# Step 1: Evaluate original
original_metrics = await _calculate_metrics_with_gt(
    images=original_images,  # ✅ 베이스 GT
)

# Step 2: Evaluate attack
if dimension == "3d":  # ✅
    attack_metrics = await _calculate_metrics_with_gt(
        images=attack_images,  # ✅ 공격 자체 GT 사용
        dataset=attack_dataset_obj,
        use_filename_mapping=False,
    )

# Step 3: Compare
robustness_metrics = calculate_robustness_metrics(
    original_metrics,  # 베이스 GT로 평가한 결과
    attack_metrics,    # 공격 자체 GT로 평가한 결과
)
```

**결과**: ✅ 각각의 GT로 평가 (베이스는 베이스 GT, 공격은 공격 GT)

---

## 🎯 수정 요약

### 변경된 파일
**`backend/app/services/evaluation_service.py:439-481`**

### 핵심 변경사항
1. ✅ **dimension 기반 분기 추가**
   - `if dimension == "3d"`: 공격 자체 GT 사용
   - `else` (2D): 원본 GT 사용 (filename mapping)

2. ✅ **3D 로직**
   - 항상 `attack_images` 사용
   - 항상 `use_filename_mapping=False`
   - `attack_dataset_obj` 사용

3. ✅ **2D 로직**
   - `original_images` 필수 체크
   - `use_filename_mapping=True`
   - filename mapping 실패 시 오류 발생

---

## 📋 테스트 체크리스트

### 2D 테스트
- [ ] 베이스만 선택 → 베이스 GT 사용 확인
- [ ] 공격만 선택 → 베이스 GT (auto-populated) 사용 확인
- [ ] 둘다 선택 → 베이스 GT로 둘 다 평가 확인
- [ ] Filename mapping 동작 확인

### 3D 테스트
- [ ] 베이스만 선택 → 베이스 GT 사용 확인
- [ ] 공격만 선택 (base 미제공) → 공격 자체 GT 사용 확인
- [ ] 둘다 선택 → 각각의 GT 사용 확인
  - [ ] 베이스 평가: 베이스 GT
  - [ ] 공격 평가: 공격 GT (CARLA 생성)
- [ ] Robustness 계산 정확성 확인

---

## 🎉 결론

**모든 요구사항 충족** ✅

1. ✅ 베이스만 선택: 2D/3D 모두 베이스 GT 사용
2. ✅ 공격만 선택:
   - 2D: 베이스 GT 사용
   - 3D: 공격 자체 GT 사용
3. ✅ 둘다 선택:
   - 2D: 베이스 GT로 둘 다 평가
   - 3D: 각각의 GT로 평가

**수정 완료 파일**:
- `backend/app/services/evaluation_service.py` (line 439-481)

**다음 단계**:
- 데이터베이스 스키마 적용
- 백엔드 재시작
- 전체 시나리오 테스트
