# 적대적 공격 보고서 생성에 필요한 라이브러리

# PDF 생성
weasyprint>=60.0

# 그래프 생성
matplotlib>=3.7.0
numpy>=1.24.0

# 이미지 처리 (WeasyPrint 의존성)
Pillow>=10.0.0

# HTML/CSS 파싱 (WeasyPrint 의존성)
cssselect2>=0.7.0
tinycss2>=1.2.0

# 폰트 렌더링 (한글 지원)
# Linux: sudo apt-get install fonts-nanum fonts-nanum-coding
# macOS: brew install font-nanum
# Windows: NanumGothic 폰트 설치 필요
